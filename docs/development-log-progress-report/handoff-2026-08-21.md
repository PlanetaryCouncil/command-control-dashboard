# Handoff 2026-08-21

Session bag for CCD / NUC / hub. Written so a new chat can
continue without the old context window.

## Rules

- Irreversible acts (public repo, delete, force-push, visibility
  flip) need the sentence *that would require making X public /
  deleting Y / force-pushing Z*, then an explicit YES / go / do
  it. A menu digit is not YES.
- Exception the operator named: in the sitting about making the
  GitHub repo public, "I want public" *was* the request. Context
  was the whole week. Card:
  https://brainfarts.planetarycouncil.org/#2026-08-20-want-is-not-go
  (Later note).
- The miss was creating `PlanetaryCouncil/hub` public without
  saying that step:
  https://brainfarts.planetarycouncil.org/#2026-08-21-irreversible-needs-yes


## Map

- `hub.planetarycouncil.org` — porch page, not a board.
  CNAME `planetarycouncil.github.io`. HTTP 200.
  Repo `PlanetaryCouncil/hub` (public; operator liked the page).
  HTTPS custom cert may still be catching up.
- `nuc.planetarycouncil.org` — cupboard board. Not live on that
  name yet.
- `gaia.planetarycouncil.org` — desk board. Not live yet.
- More nodes later, each a subdomain.
- Gaia is always on. Operator is comfy both machines public
  long-term. Hub is the intro; machines keep their own names.

## DNS / tunnel

- Namecheap `hub` CNAME is `planetarycouncil.github.io` (correct
  for the porch).
- Cloudflare tunnel `ccd-hub` still runs on the NUC
  id `01a7c58a-084a-446b-9cd4-9bc505ab4333`.
  It was aimed at hub; hub no longer points at it. Reuse for
  `nuc.` later.
- `planetarycouncil.org` is not a Cloudflare zone (NS still
  Namecheap). Live machine names via tunnel need that zone, or
  they timeout (private IPv6 only).
- Funnel still serves the NUC board on ts.net. Do not put that
  hostname on public pages.
- `genesis.re` was only the Cloudflare login key. Not the public
  name. Not `hub.genesis.re`.

## NUC

- Rota continuous: hermes only while grok SuperGrok credits
  sit at ~5%. grok spend = rare. agy spend = rare.
  `triage_agent` hermes. Telegram line follows eligible()
  so it falls to hermes. Pipeline builder still defaults
  grok — only fires if something is picked in build.txt.
- NUC Tailscale was offline (last seen ~1h) when this
  was written; Gaia's config.json holds the note until
  the cupboard pulls it.
- Daily: `git log origin/main`; latest pipeline row per proposal.
- Autotriage grok DROP 103; about 1 waiting.
- Airlock: `X-Forwarded-For` or `CF-Connecting-IP` means remote
  → 404 `/terminal`. Verified locally.
- `hub-tunnel.service` active. `ccd-board` active.

## Unfinished

- Add `planetarycouncil.org` in Cloudflare, then `nuc.` (and
  `gaia.`) as live boards, reusing the tunnel / a second tunnel.
- Do not retarget the hub CNAME back to the tunnel.
- Context monitoring: use `/context` and `/session-info` before
  85% auto-compact. Optional later: surface % on the fleet board.

## Codex checkpoint — 2026-08-22

- Loaded the existing dirty tree as shared multi-agent work; no files were
  reset, reverted, committed, pushed, or deployed.
- Full local suite reached 506 passed with 5 failures. All public-security
  server tests passed when allowed to bind localhost.
- The five failures were test isolation, not production behavior: Gaia's
  persisted `heavy-gate = off` leaked into localvoice and deferred-rota tests.
  The tests now explicitly enable heavy work inside their monkeypatched
  sandboxes; the focused verification is 8 passed.
- A final all-in-one suite rerun was interrupted, so the combined green result
  is not yet recorded as one invocation. Recommended next action: finish that
  verification, then split/commit the 21 August fleet patch before starting
  unrelated public-node work.

## Quota-flow checkpoint — 2026-08-23

- Quota routing is no longer only `plenty`/`rare`. A local ignored observation
  file records remaining percentage, observation time, and reset time without
  spending a model turn or committing account state.
- Fresh observations derive `spend`, `harvest`, `reserve`, or `exhausted`.
  Capacity nearest reset moves forward; low long-horizon capacity is held;
  exhausted capacity is never routed. Stale/missing observations cannot steer
  work and fall back to the manual table.
- Record a reading with `python3 fleet/bin/quotas.py observe AGENT PERCENT
  --resets-in-hours HOURS --source usage-command`.
- Verification: 33 quota/routing/Telegram tests passed; the remaining suite
  passed 494 tests in the sandbox, and all 22 loopback public-security tests
  passed separately with socket permission (516 collected total).
