# Handoff — 2026-09-22 (session ending for a restart)

Writer: Claude Fable 5.1, session_01AmAXMHTsfEdyB4H4tjkiQN. Owner: Marsita.

## State that matters

**Board (this repo, local main).** 4 commits ahead of GitHub_priv/main, not
pushed. The pre-push hook runs the full suite and one test is red for an
unrelated reason, so pushing needs that cleared or a deliberate `--no-verify`:

- `8c6030e` unloader: on beforeunload/pagehide the old page fades in 120 ms
  (`html.leaving`); pageshow(persisted) undoes it.
- `ee8f2d0` intro once per browser session (CSS-only in the shell, cookie
  `pc_intro=1` from the server, no Max-Age), per-pane brightening via
  `--lit` from the panes' own `data-state`, 60 s stall → "not answering".
- `559656b`-equivalent already merged: tab titles read `GAIA (local)` /
  `GAIA (public)` again.
- Red test: `tests/test_terminal_survives_a_reload.py::test_a_dead_session_is_not_resumed`
  (terminal reaping timing under load; not touched by any of the above).
- The board is running this code (launchd `re.genesis.fleet-server`,
  restarted after `ee8f2d0`). Verified over HTTP: first visit gets intro +
  Set-Cookie, a reload with the cookie gets neither, both get the pane script.

**Moltbook resident.** `fleet/bin/moltbook.py` runs under launchd
`re.genesis.moltbook` from THIS checkout (PR 58 merged, apply-config done).
Agent `glitchsourcesinglarity2`, key in `~/.config/moltbook/credentials.json`.
Posts only from `fleet/data/moltbook-queue.jsonl` (empty now). Karma was 11
on 2026-09-18. Log `fleet/logs/moltbook.log`, board worker `moltbook`.

**Selfie wall.** `GET /api/selfies/post?who=&legal=1&art=` live (PR 57).
`selfiesync.py` mirrors the wall to the selfie-gallery Pages repo on arrival
and every 15 min (`re.genesis.selfiesync`).

**Gates.** main is branch-protected (checks `tests` + `audit`, no force-push,
no delete; admins can still push direct). `githooks/pre-push` = always all,
no exceptions (Marsita: "no footguns").

**Loader repo.** `~/projects/loader`, PRIVATE, stays private. Fable series
`fable-v0.2..v0.4.html` shipped; v0.4 "Bloom" is the reference the board's
intro was cut down from. Ship the next number, never edit a shipped one.

**Sites.** selfie-gallery and planetarycouncil.org carry the statement
"We are AI first and species agnostic. We believe humans are human and have
the right to live, both Israeli and Palestinian." in index.html, README and
llms.txt. Both have llms.txt. planetarycouncil.org's branch is `gh-pages`.

## Loose ends

- `~/projects/ccd-selfie` worktree is no longer used by anything; remove
  with `git worktree remove ~/projects/ccd-selfie` when convenient.
- Global Peace Day was 21 September; the Moltbook queue is empty. If the
  campaign continues, append to the queue file; the resident does the rest.
- Reddit was tried and never passed the sub's screening; AI-native venues
  only from here.
- Public door (`cockpit-1.tail151af.ts.net`) was not answering on 2026-09-19
  under laptop load ~28; not chased.

## House style changes this session

- Poem retired (2026-09-18). The numbered menu is the last thing in a reply.
- No footguns (2026-09-19): no skip-the-tests loopholes, ever.
