{
  "_comment": "Fleet schedule. Edit, then run: bash bin/apply-config.sh",
  "heartbeat": {
    "_what": "One relay check for every agent. Hand an agent a random number, it must reply with that number plus one, then pass it on \u2014 a correct reply is only possible if the previous value arrived.",
    "every_seconds": 86400,
    "agents": [
      "hermes",
      "grok",
      "agy"
    ],
    "laps": 1,
    "_cost_note": "Once a day, not every 30 minutes. Measured 2026-08-02: one full relay costs ~960s of wall clock. At 1800s it occupied over half of all wall-clock time on a four-core machine, and the fleet's own load average reached 20 \u2014 five times the cores. The relay answers one question (can the agents pass a message), the answer changes rarely, and re-asking it hourly was the single largest consumer of a machine that has none to spare.",
    "max_load": 6.0,
    "_load_note": "Skip the run when the 1-minute load average is above this. The check costs 2.4 microseconds against the 960 seconds it guards \u2014 the cheapest thing on the board protecting the most expensive. Agents timing out under load is not a comms failure, and recording it as one is how three councils ended up diagnosing the wrong thing."
  },
  "watchdogs": {
    "_what": "Run each monitored project's own test suite.",
    "every_seconds": 3600
  },
  "self_improve": {
    "_what": "Mine session transcripts, propose tooling, commit only what survives.",
    "at_hour": 3,
    "at_minute": 0
  },
  "pipeline": {
    "_what": "Closes the rota's loop: unprocessed proposals are implemented by grok (or FLEET_BUILDER) on rota/* branches in worktrees, pytest runs, hermes (a different vendor) reviews each diff, and the board lists what awaits a human merge. Nothing merges itself. Autopilot triage (agy, cheap) drops errors and duplicates so telegram does not count 1800 restatements. Building still needs a pick in build.txt.",
    "every_seconds": 3600,
    "triage_agent": "hermes"
  },
  "council": {
    "_what": "Agents take turns proposing workflow improvements. They read the board, decide for themselves, and write to a shared transcript. Nothing is applied \u2014 a human reads and decides.",
    "every_seconds": 10800,
    "agents": [
      "claude",
      "hermes",
      "grok",
      "agy"
    ],
    "rounds": 2,
    "_cost_note": "Each turn is one agent for ~20-100s, one at a time. Four agents x two rounds is roughly 8 turns \u2014 double what it was. Every 3 hours still keeps the machine alone between sessions. ollama is the expensive one: dolphin-llama3 holds several GB resident while it thinks, on a box with 8. If the load gate starts deferring everything, drop ollama first and rounds second.",
    "_vendor_note": "claude=anthropic, grok=xai, agy=google, hermes=openai, openclaw=openai, ollama=local. Sitting roster is three companies (openai, xai, google). claude stays in the chat pane and comes back onto the schedule when the Anthropic plan has credits again. ollama is the only one that keeps working with the wifi off, and it is out of the council on this hardware.",
    "_ollama_note": "In the council: no, on this hardware. Measured across 20 turns (2026-08-05): median 169s, worst 712s, and ONE turn in twenty that was its own thought \u2014 the rest echoed a prompt heading, copied the worked example verbatim, or critiqued a line of its own input. Three prompt rewrites did not move it. qwen2.5:3b was tried on the same brief: 564s and a fragment. A council turn needs ~1,500 tokens of context, and prefill on this 4-core box runs ~5 tok/s, so the reading alone costs minutes before a word is generated. The reason to want a local voice is unchanged \u2014 it is the only one that survives the wifi dropping \u2014 so ollama stays RUNNING and stays in the chat pane, where a human asks a direct question and can wait. It is out of the council, where it cost 3-10 minutes a sitting and contributed once."
  },
  "e2e": {
    "_what": "End-to-end check against live infrastructure, with canaries proving each path was actually traversed. Includes real agent turns.",
    "at_hour": 5,
    "at_minute": 30,
    "_cost_note": "Two agent turns per run. Daily is enough to catch drift; hourly would spend turns to learn nothing new. The --quick variant runs on every merge via githooks/post-merge and costs nothing."
  },
  "rota": {
    "_what": "One agent at a time, in rotation, reads the board and proposes one concrete project action. Proposes only \u2014 never applies. Hourly on the NUC: enough to keep ideas flowing without flooding triage or draining one provider's quota.",
    "every_seconds": 3600,
    "agents": [
      "hermes",
      "grok",
      "agy"
    ],
    "_cost_note": "Hourly, one agent per firing \u2014 not all three. A turn is 20-170s and one core. A 60-second continuous gap produced 4,304 turns and 4,325 proposals by 2026-08-25, mostly repetitive Hermes output; hourly cadence is the quota and triage safety boundary."
  },
  "board_medic": {
    "_what": "Probes the board on :8787 every five minutes; two silent probes trigger a fleet-server kickstart, a failed revival raises needs_you. The heartbeat medic, applied to the board itself.",
    "every_seconds": 300
  },
  "quotas": {
    "_what": "Quota is a flow: fresh remaining/reset observations steer work toward capacity that would otherwise expire, low capacity is reserved, and stale/missing observations fall back to the manual spend table. The pulse never calls a model.",
    "observation_max_age_hours": 48,
    "reserve_below_pct": 10,
    "harvest_within_hours": 24,
    "spend": {
      "grok": "rare",
      "agy": "rare",
      "hermes": "plenty",
      "claude": "rare",
      "openclaw": "rare"
    }
  },
  "local_voice": {
    "_what": "One short question a day to the local model, so the offline fallback is known-good before the day the wifi dies. It is not an agent here: this laptop is too slow for local inference as work (measured 2026-08-05 \u2014 a council turn cost 3-10 minutes and said nothing). It is a backup with a pulse.",
    "at_hour": 6,
    "at_minute": 15
  },
  "report": {
    "_what": "One page a day saying what the fleet actually did, published to this repo's own GitHub Pages. Written 2026-09-03, after a day that surfaced three failures \u2014 nothing built for two days, the heartbeat dead for 31 hours, the watchdog tier never once run on the NUC \u2014 each of them visible in some log and none of them visible anywhere a person looks. 19:00 so it covers a working day rather than cutting one in half.",
    "at_hour": 19,
    "at_minute": 0
  },
  "daily": {
    "_what": "The one message the operator actually reads, sent to Telegram at 09:00. Distinct from `report`, which publishes a page at 19:00 -- one is pushed to a phone in the morning, the other is a link that waits. It ran for weeks as a hand-written systemd unit that apply-config did not generate, so neither machine could be repaired from the repo. Generated now.",
    "at_hour": 9,
    "at_minute": 0
  },
  "builders": {
    "_comment": "One builder at a time. gap_seconds counts from when the last slot FINISHES, so slots cannot overlap however long one runs. max_seconds bounds a slot that hangs. Was three template instances at 30s with a 4h timeout, which pinned six cores for a day and built nothing.",
    "gap_seconds": 300,
    "max_seconds": 1800
  }
}
